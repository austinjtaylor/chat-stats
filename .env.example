# Environment Variables Template
# Copy this to .env and fill in your actual values

# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
# Set to "production" in Railway, "development" locally
# This affects security headers, logging format, and allowed hosts
ENVIRONMENT=development

# =============================================================================
# REQUIRED: Anthropic API (for AI features)
# =============================================================================
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your_key_here

# =============================================================================
# REQUIRED: SUPABASE (PostgreSQL Database + Authentication)
# =============================================================================
# Get these from your Supabase project dashboard (Settings → API)
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# REQUIRED: Database connection string (Settings → Database → Connection String → URI)
# This is used for both development and production
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxxxxxxxxx.supabase.co:5432/postgres

# REQUIRED: JWT Secret for token verification (Settings → API → JWT Settings → JWT Secret)
# This is the "Legacy JWT secret" shown in your Supabase dashboard
JWT_SECRET=your-jwt-secret-here

# =============================================================================
# STRIPE (Payment Processing)
# =============================================================================
# Get these from https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_/pk_test_) for development
# Use live keys (sk_live_/pk_live_) for production
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Webhook secret (from Stripe Dashboard → Webhooks)
# For local testing: use `stripe listen --forward-to localhost:8000/api/stripe/webhook`
STRIPE_WEBHOOK_SECRET=whsec_...

# Price IDs for subscription tiers (from Stripe Dashboard → Products)
# Get these after creating products in your Stripe dashboard
STRIPE_PRO_PRICE_ID=price_...

